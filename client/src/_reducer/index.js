import { combineReducers } from 'redux';
//import user from './user_reducer';
//특정 state에 해당하는 reducer를 가져와서 rootReducer로 합쳐준다.

const rootReducer = combineReducers({
  //user,
});

export default rootReducer;
